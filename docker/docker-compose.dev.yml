# caspaste — development compose
# Usage: docker compose -f docker/docker-compose.dev.yml up -d
# Development compose - HUMAN USE ONLY (never used by AI)

name: caspaste-dev

x-logging: &default-logging
  options:
    max-size: '5m'
    max-file: '1'
  driver: json-file

services:
  caspaste:
    image: ghcr.io/casjay-forks/caspaste:devel
    container_name: caspaste-dev
    hostname: ${BASE_HOST_NAME:-$HOSTNAME}
    restart: unless-stopped
    pull_policy: always
    logging: *default-logging
    environment:
      - MODE=development
      - PORT=80
      - DEBUG=true
      - TZ=${TZ:-America/New_York}
      - CASPASTE_DB_DRIVER=sqlite
    ports:
      - '8080:80'
    volumes:
      - './volumes/config:/config:z'
      - './volumes/data:/data:z'
    healthcheck:
      test: /usr/local/bin/caspaste --status
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 60s
    networks:
      - caspaste-dev

networks:
  caspaste-dev:
    name: caspaste-dev
    external: false
